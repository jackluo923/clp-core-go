# Build artifacts
*.o
*.a

# Bazel symlinks
bazel-*

# Generated by `go generate` / CI — committed to the release branch only,
# never to main. The release branch CI uses `git add -f` to override these.
embed_linux_amd64.go
embed_linux_arm64.go
